# {{项目名}} — 给执行 AI 的工作手册

> 本文件刻意保持一页以内。它是索引和红线，不是执行器——真正拦截错误的是
> `.claude/settings.json`（动作拦截）、`scripts/check-rules.mjs`（代码检查）和测试。
> 每条新教训先问：能不能变成机器规则？能就去改那三处，不能才写进这里。

## 项目信息（开工时填写）

- **产品一句话**：{{做什么、给谁用}}
- **技术栈**：{{如 Next.js + Supabase + Vercel}}
- **部署命令**：{{如 vercel --prod；数据库迁移走什么通道}}
- **锁定决策**（已定不再讨论的事，逐条列）：
  - {{例：计费只用一种货币单位，禁止双轨}}
  - {{例：取数只走 X 通道，不要提议 Y}}

## 六条红线（违反任何一条 = 立即停下）

1. **不可逆动作（发帖 / 付款 / 发邮件 / 代发）失败后绝不自动重试**——先查真实终态，然后停下等人工。
2. **合并、部署、发布逐次授权**——每一次都单独问过用户，上次同意不算数（settings.json 已强制，别绕）。
3. **跟钱有关的数字一律服务端取**——价格 / 成本 / 上限绝不接受客户端传值，金额入参必须有负数守卫。
4. **写数据库必须检查写入结果**——check-rules 会拦，被拦了就修，不许加豁免注释绕过。
5. **声明"验证通过"必须说明层级**——只跑了类型检查和单测 = 结构性验证；连真库、开真页面 = 行为性验证。只有前者时必须明说。
6. **动数据库 / 架构的任务，先复述理解再动工**——交出「我理解你要做什么 + 我会改哪些地方」，用户确认后才写代码。

## 工程约定（按项目补充）

- 每个表单配「打回后已填内容还在」的测试（模板见 `templates/tests/`）。
- 文件上传走浏览器直传对象存储，并配一条超平台限额的大文件测试。
- 批量调用 LLM：按业务 ID 对齐返回、假设会漏答、配乱序/漏答测试。
- 接按量计费 API：数量参数在代码里写死封顶值 + 测试锁住；只有"未计费"错误才允许重试。
- 外部数据源返回空结果要留痕（为什么空），连续空要告警——「成功且空」按事故处理。
- 排障顺序：先查环境（env 覆盖 / 依赖重复 / 构建产物 / 端口进程），再怀疑业务代码。
- {{其余按项目补充，保持简短}}
